/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.prod.website-files.com','www.branchfurniture.ca', 'smiletwice.com', 'www.pngall.com', 'randomuser.me', 'picsum.photos'],
    }
};

export default nextConfig;
