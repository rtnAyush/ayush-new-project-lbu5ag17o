/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fastly.picsum.photo','cdn.prod.website-files.com','www.branchfurniture.ca', 'smiletwice.com', 'www.pngall.com', 'dalleproduse.blob.core.windows.net', 'media.istockphoto.com', 'randomuser.me', 'picsum.photos', 'images.unsplash.com', 'cdn.midjourney.com', 'picsum.photos'],
    }
};

export default nextConfig;
